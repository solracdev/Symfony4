<h3>Welcome to the app!</h3>

Hello {{ user.fullName }}! You've registered into the app! <br><br>

{# La funcion de twig URl, genera la ruta ABSOLUTA a partir del nombre que tengamos en un controlador #}
Please confirm your account by clicking this <a href="{{ url("security_confirm", {"token": user.confirmationToken}) }}">link.</a> 

{# La funcion de twig URl, genera la ruta ABSOLUTA a partir del nombre que tengamos en un controlador #}
To Login, go to <a href="{{ url("security_login") }}">login page</a>.<br><br>

Thanks !