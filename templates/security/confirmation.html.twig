{% extends "base.html.twig" %}

{% block body %}

    {# Comprobar que el usuario pasado por parametro no es null #}
    {% if user %}
        <h5>Congratulations!</h5>
        <p>Your account is confirmed! You can now sing in <a href="{{ path("security_login") }}">here</a>.</p>
    {% else %}
        Something went wrong. Please check your e-mail.
    {% endif %}
    
{% endblock %}