{# blod/post.html.twig Template #}

{% extends "base.html.twig" %}

{% block title %}{{ post.title }}{% endblock %}

{% block body %}
        <p>ID {{ id }}</p>
        <h3>{{ post.title }}</h3>
        <p>{{ post.text }}</p>
        <a href="{{ path("blog_index") }}">Back</a>
        
{% endblock %}